{"input": "Show me how to filter even numbers", "expected_output": "You can filter even numbers with a list comprehension like this: [x for x in range(10) if x % 2 == 0]. The if clause acts as a filter.", "metadata": {"topic": "list comprehensions", "difficulty": "easy"}}
{"input": "How do I create a dictionary from two lists?", "expected_output": "Use the zip() function with dict(): keys = ['a', 'b'], values = [1, 2], result = dict(zip(keys, values)). This creates {'a': 1, 'b': 2}.", "metadata": {"topic": "dictionaries", "difficulty": "easy"}}
{"input": "Explain how to read a CSV file into a list of dictionaries", "expected_output": "Use the csv module: import csv, then with open('file.csv') as f: reader = csv.DictReader(f), data = [row for row in reader]. Each row becomes a dictionary with column headers as keys.", "metadata": {"topic": "file I/O", "difficulty": "medium"}}
{"input": "What's the difference between a tuple and a list?", "expected_output": "Tuples are immutable (cannot be changed after creation) while lists are mutable. Tuples use () and lists use []. Tuples are faster and can be used as dictionary keys.", "metadata": {"topic": "data structures", "difficulty": "easy"}}
{"input": "How do I handle exceptions in Python?", "expected_output": "Use try/except blocks: try: risky_code() except ValueError as e: handle_error(e). You can also use finally for cleanup code that always runs.", "metadata": {"topic": "exception handling", "difficulty": "medium"}}
